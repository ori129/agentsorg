# Claude Code Instructions

## Autonomy

Proceed without asking for confirmation on all actions **except delete operations** (deleting files, dropping DB tables, removing branches, destructive git ops). For everything else — editing files, running commands, creating files, making API calls, building, deploying locally — just do it.
